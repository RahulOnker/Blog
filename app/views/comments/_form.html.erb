<%= form_for([@chat, @comment]) do |f| %>

    <strong><%= f.label :Add_comment %></strong>
    <%= f.text_field :comment %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    
    <%= f.submit "create comment" %>

<% end %>