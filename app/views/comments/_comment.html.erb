<% @comments.each do |c| %>

    <p>
       <strong><%= c.user.email %> -</strong>
        <%= c.comment %>
        <%= link_to "Delete", chat_comment_path(c.chat_id, c.id), method: :delete %>
    
    </p>



<% end %>